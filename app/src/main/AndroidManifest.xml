<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.android.webspidola"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- permission android:name="android.permission.MEDIA_CONTENT_CONTROL"/ -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- android:configChanges="orientation|screenSize|keyboardHidden" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name=".RemoteControlReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>

        <service
            android:name=".MediaPlayerService"
            android:exported="false">
            <receiver android:name=".BroadcastNotificationReceiver">
                <intent-filter>

                    <!--
                    action android:name="media.player.service.action_play" >
                    </action>
                    <action android:name="media.player.service.action_pause" >
                    </action
                    -->
                    <action android:name="android.intent.action.ACTION_HEADSET_PLUG"/>

                    <category android:name="android.intent.category.DEFAULT"/>
                </intent-filter>
            </receiver>
        </service>

        <!--
        activity
            android:name=".NotificationAreaActivity"
            android:label="@string/title_activity_notification_area"
            android:theme="@style/AppTheme.NoActionBar">
        </activity
        -->
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings">
        </activity>
    </application>

</manifest>
